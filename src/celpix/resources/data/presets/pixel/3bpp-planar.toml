# 3bpp per-row interleaved planar — SMS/GG player graphics.
# See docs/graphics-formats-reference/implementation-guide.md §2 (planar plane-offset table).
id = "preset.pixel.3bpp-planar"
name = "3bpp planar, row-interleaved (8x8)"
engine_id = "codec.pixel.planar"
category = "Generic planar"

[params]
bpp = 3
planes = [
  { base = 0, stride = 3 },
  { base = 1, stride = 3 },
  { base = 2, stride = 3 },
]
