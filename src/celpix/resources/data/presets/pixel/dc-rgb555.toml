# Direct-color RGB555 direct (8x8).
# See docs/graphics-formats-reference/implementation-guide.md §3 (direct-color tiles).
# 15 significant bits in a 2-byte pixel.
id = "preset.pixel.dc-rgb555"
name = "Direct RGB555 (8x8)"
engine_id = "codec.pixel.direct-color"

[params]
bytes_per_pixel = 2
byte_order = "little"

[params.masks]
r = 0x7C00
g = 0x03E0
b = 0x001F
