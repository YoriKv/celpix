# Big-endian variant of BGR555 direct (8x8) — for big-endian ROM data (e.g. N64).
# See docs/graphics-formats-reference/implementation-guide.md §3 (direct-color tiles).
id = "preset.pixel.dc-bgr555-be"
name = "BGR555 direct (8x8) (big-endian)"
engine_id = "codec.direct-color"

[params]
bpp = 15
bytes_per_pixel = 2
byte_order = "big"

[params.masks]
r = 0x001F
g = 0x03E0
b = 0x7C00
