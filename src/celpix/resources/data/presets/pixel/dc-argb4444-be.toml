# Big-endian variant of ARGB4444 direct (8x8) — for big-endian ROM data.
# See docs/graphics-formats-reference/implementation-guide.md §3 (direct-color tiles).
id = "preset.pixel.dc-argb4444-be"
name = "Direct ARGB4444 (big-endian)"
engine_id = "codec.pixel.direct-color"
category = "Direct color"

[params]
bytes_per_pixel = 2
byte_order = "big"
fields = "aaaa rrrr gggg bbbb"
