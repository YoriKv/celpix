# SNES 8bpp whole-bank — the tile is the entire 128x128 bank (16384 bytes).
# One byte per pixel, so this is codec.packed at the depth where a field fills a
# whole byte.
# See docs/graphics-formats-reference/implementation-guide.md §2 (chunky).
id = "preset.pixel.snes-8bpp-bank"
name = "SNES 8bpp whole-bank (128x128)"
engine_id = "codec.packed"

[params]
bpp = 8
tile_width = 128
tile_height = 128
