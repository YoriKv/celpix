"""Per-launch undo/redo history (``docs/design/overview.md`` §6).

History is kept in memory for the running app and is not persisted to the
project file. Undoable state includes both edits and stage reconfiguration.

Intentionally empty for now.
"""
